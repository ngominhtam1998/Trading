# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/

# State databases (trading state, not code)
production/live/state_*.db
production/live/state_*.db-wal
production/live/state_*.db-shm

# Logs
production/live/bot_*.log
*.log

# Cache files
*.pkl
*.cache

# API keys (NEVER commit)
.env
.env.local
*.key
*.pem

# OS
Thumbs.db
.DS_Store
desktop.ini

# Data files (large CSV, fetched on-demand)
*.csv

# Backtest reports (generated, not source)
reports/*.csv

# Top symbol lists (generated)
all_usdt_perpetuals.json
